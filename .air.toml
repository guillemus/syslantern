root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "tmp/server"
  full_bin = "./tmp/server"
  include_ext = ["go", "sql"]
  exclude_dir = ["tmp", "node_modules", "data", ".git"]
  delay = 1000
  stop_on_error = true

[log]
  time = true
